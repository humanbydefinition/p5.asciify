import { P5Asciifier } from '../Asciifier';
/**
 * Registers the `setupP5Instance` and `setupAsciifier` methods to the p5.js instance.
 */
export declare function registerSetupMethods(p5asciify: P5Asciifier): void;
